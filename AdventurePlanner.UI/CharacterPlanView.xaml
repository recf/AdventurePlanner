<Controls:MetroWindow x:Class="AdventurePlanner.UI.CharacterPlanView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"        
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      Title="Adventure Planner" 
                      Height="350" 
                      Width="525">
    <Controls:MetroWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibility"></BooleanToVisibilityConverter>
    </Controls:MetroWindow.Resources>
    
    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <Label Content="*" Visibility="{Binding Path=IsDirty, Converter={StaticResource BooleanToVisibility}}"/>
            <Button Content="Load" Command="{Binding Load}"/>
            <Button Content="Save" Command="{Binding Save}"/>
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>
    
    <Grid>

        <StackPanel>
            <Label Content="Changed"/>
            <CheckBox x:Name="IsDirty" IsEnabled="False"/>
            
            <Label Content="Name"/>
            <TextBox x:Name="CharacterName"/>

            <Label Content="Race"/>
            <TextBox x:Name="Race"/>
        </StackPanel>
        
    </Grid>
</Controls:MetroWindow>
