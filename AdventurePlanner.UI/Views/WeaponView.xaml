<UserControl x:Class="AdventurePlanner.UI.Views.WeaponView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        
        <Label Content="Name"/>
        <TextBox Text="{Binding Name}"/>

        <Label Content="Proficiency Group"/>
        <TextBox Text="{Binding ProficiencyGroup}"/>
        
        <Label Content="Damage Dice"/>
        <TextBox Text="{Binding DamageDice, Converter={StaticResource DiceRollConverter}}"/>

        <Label Content="Damage Type"/>
        <TextBox Text="{Binding DamageType}"/>

        <Label Content="Normal Range"/>
        <controls:NumericUpDown Value="{Binding NormalRange}" Minimum="0" Interval="5"/>
        
        <Label Content="Max. Range"/>
        <controls:NumericUpDown Value="{Binding MaximumRange}" Minimum="0" Interval="5"/>

        <CheckBox IsChecked="{Binding HasAmmunition}" Content="Has Ammunition?"/>

        <CheckBox IsChecked="{Binding IsLight}" Content="Light"/>

    </StackPanel>
</UserControl>
