<UserControl x:Class="AdventurePlanner.UI.Views.FeaturePlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <CheckBox Grid.Column="0" x:Name="IsSelected" />
        <TextBox Grid.Column="1" x:Name="FeatureName" controls:TextboxHelper.Watermark="Feature" />
        <TextBox Grid.Column="2" x:Name="Description" 
                 TextWrapping="WrapWithOverflow"
                 controls:TextboxHelper.Watermark="Description"/>
    </Grid>
</UserControl>
